The images are currently loading slowly and rendering progressively from top to bottom even though Cloudflare R2 CDN is being used. Please identify and fix the actual performance bottlenecks in the frontend image delivery and rendering pipeline.

This is a React/Vite based custom T-shirt customization platform using Cloudflare R2 for asset storage and CDN delivery. The issue is most noticeable with product images, transparent PNG textures, hero banners, and customizer assets.

Please implement a complete image optimization solution with the following improvements:

1. Optimize Image Upload Flow

* Compress images before upload on the frontend
* Use browser-side image compression
* Prevent users from uploading excessively large files
* Resize images automatically before upload
* Keep upload quality visually high while reducing size significantly

2. Convert Images to Efficient Formats

* Automatically convert supported images to WebP
* Use PNG only when transparency is required
* Reduce file sizes while maintaining visual quality

3. Improve Image Loading Performance

* Prevent the “top-to-bottom slow rendering” effect
* Ensure images load smoothly and fully
* Optimize browser decoding and rendering
* Reduce layout shifts and rendering lag

4. Implement Lazy Loading

* Add lazy loading for product and gallery images
* Load only visible images initially
* Improve initial page speed and performance

5. Add Proper Caching

* Configure optimal cache headers for Cloudflare R2 assets
* Use long-term immutable caching for static assets
* Prevent unnecessary re-downloads

6. Optimize T-Shirt Customizer Assets

* Optimize texture files used in the customizer
* Reduce large transparent PNG overhead
* Ensure textures load quickly inside canvas/WebGL/Three.js rendering
* Keep texture sizes optimized (recommended under 2048x2048)

7. Add CDN/Image Resizing Support

* Integrate Cloudflare Image Resizing or equivalent optimization
* Serve responsive image sizes dynamically
* Deliver smaller images for mobile devices

8. Performance Goals
   Target the following:

* Product thumbnails under 300KB
* Hero banners under 500KB
* Texture files under 1MB
* Faster first image render
* Smooth loading without progressive/top-to-bottom visual artifacts

9. Debug & Analyze

* Inspect image decode/render bottlenecks
* Analyze network loading performance
* Identify oversized assets causing delays
* Optimize wherever performance issues exist

Please update the existing implementation completely and ensure the final result provides fast, production-grade image loading performance across desktop and mobile devices while maintaining high visual quality.
