Here is the full gap analysis, then the corrected and aligned final prompt:

---

## Gap Analysis — Prompt vs FRD

| # | FRD Requirement | In Previous Prompt | Status |
|---|---|---|---|
| 1 | Persistent bottom nav: Back / Continue / Add to Cart on every step | ✅ Covered | Match |
| 2 | Progress tracker: STYLE → DESIGN → LOGO & TEXT → SIZING MATRIX | ⚠️ Not explicitly adapted for SKU-conditional flows | Gap |
| 3 | On-screen instruction text above each step panel | ❌ Missing exact instructions per step | Gap |
| 4 | Step 1 — Style selector cards (Solid / Print / Pattern 1 / Pattern 2) | ⚠️ Replaced by SKU auto-load — but step label and tracker must still show | Gap |
| 5 | Step 2 — Conditional Logic A: Solid/Print → "Apply to All" one zone | ✅ Covered | Match |
| 6 | Step 2 — Conditional Logic B: Pattern → Base Body + Pattern Panels separately | ✅ Covered | Match |
| 7 | Color modal: quick swatches (White/Black/Navy/Green/Red) + color wheel + eyedropper side by side | ⚠️ Mentioned but layout detail missing | Gap |
| 8 | Print modal: 3-column swatch grid, hover preview, Apply/Cancel | ⚠️ Mentioned but grid layout and hover not specified | Gap |
| 9 | Step 3 — Logo: drag within fixed chest safe zone, resize handles, background removal button | ✅ Covered | Match |
| 10 | Step 3 — Text: font dropdown + size slider + color picker + L/C/R alignment toggles | ✅ Covered | Match |
| 11 | Step 3 — Use thumbnail images to show which shirt part is being targeted | ✅ Covered | Match |
| 12 | Step 4 — Toggle between Standard Sizes grid and Custom Specification form | ✅ Covered | Match |
| 13 | Step 4 — S/M/L/XL/XXL quantity inputs with running total | ✅ Covered | Match |
| 14 | Step 4 — WhatsApp bulk order CTA callout | ✅ Covered | Match |
| 15 | All state preserved when navigating Back | ✅ Covered | Match |
| 16 | Admin SKU upload panel | ✅ Covered (from user requirement) | Match |
| 17 | Left preview panel = 40% on large screens | ✅ Covered (from user requirement) | Match |
| 18 | SKU-based conditional page flow (KP/KS/Solid) | ✅ Covered (from user requirement) | Match |
| 19 | Cart captures full customization details | ✅ Covered | Match |
| 20 | Progress tracker step labels adapt to SKU flow | ❌ Missing | Gap |

---

## Final Corrected & Complete Prompt

---

The app is already built. Do not change the existing code structure or rebuild anything. Apply only the following targeted changes to the existing codebase:

---

**1. Admin — SKU-Based Design Upload Panel**

Add an admin panel where the admin can enter a SKU and upload the corresponding design asset (pattern layer PNGs, print image, or solid base). Tag each SKU as one of three product types: Pattern, Print, or Solid. All SKU-to-design mappings must persist in the database so future uploads work immediately without code changes. The customization page must read the incoming SKU from the URL or product page and load the correct registered design automatically.

SKU type identification rules:
- SKU starting with `KP` = Pattern product
- SKU starting with `KS` = Print product
- SKUs tagged as Solid by admin = Solid product

---

**2. Layout — Left Preview Panel Fixed at 40%**

On large screens (desktop and tablet landscape), the live garment preview panel must occupy exactly 40% of the screen width on the left. The customization control panels occupy the remaining 60% on the right. The entire customization page must be fully responsive across mobile, tablet, and desktop screen sizes without breaking any layout.

---

**3. Persistent Progress Tracker — Adapts Per SKU Flow**

A horizontal progress tracker must remain visible at the top of the screen at all times across every step. The labels are:

```
1. STYLE  ———  2. DESIGN (COLOR/PRINT)  ———  3. LOGO & TEXT  ———  4. SIZING MATRIX
```

The active step must be highlighted with a glow or filled indicator. Completed steps show a check. For Pattern and Print SKUs, Step 1 is auto-completed on page load since the design is pre-loaded from the SKU — the tracker must reflect this by marking Step 1 as done immediately and placing the user at Step 2. For Solid SKUs, Step 1 shows the customization type choice options and is marked done once the user selects one.

---

**4. Persistent Bottom Navigation Bar — Every Step**

A fixed bottom bar must appear on every step containing:

```
[ ← Back ]     [ Continue to Next Step → ]     [ 🛒 Add to Cart ]
```

- Back is disabled on the first active step
- Continue label changes to "Review Order" on Step 4
- All state is preserved when navigating Back — no fields reset, no selections clear

---

**5. On-Screen Instruction Text — Each Step**

Display a clear instruction line above the control panel on every step:

- Step 1 (Solid SKU only): *"Start your design! Choose how you want to customize your t-shirt."*
- Step 2: *"Color your canvas. Click on a garment section to choose a vibrant solid color or a premium print pattern."*
- Step 3: *"Make it uniquely yours. Upload your team logo or add custom text. Drag and resize your assets freely within the highlighted chest area."*
- Step 4: *"Select your sizing preferences and enter the quantity needed for each size to complete your order."*

---

**6. Page Flow — SKU-Driven Conditional Navigation**

Everything shown to the user on the customization page must be driven entirely by the SKU passed in from the product page. Never show options that are irrelevant to the selected product type.

---

**FLOW A — Pattern Product (SKU prefix: KP, example: KP1001B)**

**Step 1 — Auto-loaded and skipped:**
The design registered to this SKU loads instantly in the live preview. Step 1 is marked complete. The user is placed directly at Step 2.

**Step 2 — Choose Customization Type:**
Show the user two clearly labelled option cards:

- **Color Customization**
  → Show two zone buttons:
  - Base Body → opens Color Modal (user picks body color)
  - Pattern Panels → opens Color Modal (user picks accent color for collar, sleeves, and panels simultaneously)
  → Instruction: *"Color your canvas. Click on a garment section to choose a vibrant solid color."*

- **Print Customization**
  → Show the pattern parts with a "Choose Print" button per panel zone
  → Opens the Print Swatch Gallery Modal (3-column grid, gp001–gp034 or all admin-uploaded prints, hover preview, Apply/Cancel)
  → The selected print is applied on top of the pattern panels in the live preview
  → Instruction: *"Select a premium print to apply across your pattern panels."*

**Step 3 — Logo & Text** (as specified in Section 8 below)

**Step 4 — Sizing & Order** (as specified in Section 9 below)

---

**FLOW B — Print Product (SKU prefix: KS, example: KS1000BGP001)**

**Step 1 — Auto-loaded and skipped:**
The design registered to this SKU loads instantly in the live preview. Step 1 is marked complete. The user is placed directly at Step 2.

**Step 2 — Print Selection:**
Show the full print gallery — all prints from gp001 to gp034 plus any admin-uploaded prints — in a 3-column thumbnail grid. The currently applied print (from the SKU) is pre-selected and highlighted. User can switch to a different print. The live preview updates instantly on hover or selection. Modal has Apply and Cancel buttons.

Instruction: *"Color your canvas. Select a premium print pattern to apply to your t-shirt."*

**Step 3 — Logo & Text** (as specified in Section 8 below)

**Step 4 — Sizing & Order** (as specified in Section 9 below)

---

**FLOW C — Solid Product**

**Step 1 — Choose Customization Type:**
The base solid design from the SKU loads in the live preview. Show the user three clearly labelled option cards to choose how they want to customize:

- **Pattern Customization**
  → Proceed with the exact same flow as FLOW A Step 2 onward (Color or Print sub-choice for pattern panels)

- **Print Customization**
  → Proceed with the exact same flow as FLOW B Step 2 onward (full print gallery applied to entire shirt)

- **Color Customization**
  → Show two sub-options:
  - **Full Body Color** — opens Color Modal, applies one color across the entire shirt
  - **Parts Color** — shows zone buttons per shirt section (collar, sleeves, front body, back body), each opens Color Modal independently

Instruction: *"Start your design! Choose how you want to customize your t-shirt."*

**Step 2 — Customization controls** based on whichever type was chosen above

**Step 3 — Logo & Text** (as specified in Section 8 below)

**Step 4 — Sizing & Order** (as specified in Section 9 below)

---

**7. Color Modal — Exact Layout**

When any color zone button is tapped, open a modal overlay with this exact layout:

- **Row 1 — Quick Colors:** Five circular swatches in a row — White, Black, Navy, Green, Red. Tapping one instantly previews on the live shirt.
- **Row 2 — Two columns side by side:**
  - Left column: Interactive color spectrum canvas (full color wheel or gradient picker)
  - Right column: Eyedropper / hex code input field
- **Footer:** Cancel button (left) and APPLY button (right, filled accent color)

Closing the modal without clicking Apply must discard the change and restore the previous color.

---

**8. Print Swatch Gallery Modal — Exact Layout**

When any print zone button is tapped, open a modal overlay with this exact layout:

- **Header:** "Select From Premium Print Patterns"
- **Body:** 3-column responsive grid of print thumbnail cards. Each card shows the print image. Hovering shows a larger preview. The currently selected print has a highlighted border.
- **Footer:** Cancel button (left) and APPLY button (right)

The grid must include all prints from gp001 to gp034 plus any prints uploaded by the admin via SKU. Closing without Apply discards the selection.

---

**9. Step 3 — Logo & Text Section**

**Logo Upload:**
- Upload button labelled "⬆ Upload Asset Image"
- Accepted formats: PNG (with transparency), JPG
- Uploaded logo renders inside a fixed dashed chest safe zone on the live preview
- Logo is draggable within the safe zone only — cannot be moved outside the chest boundary
- Resize handles on the logo corners allow scaling within the safe zone
- One-click "✦ Remove Image Background" button below the upload area

**Text Customization:**
- Text input field for custom text entry
- Font dropdown populated with sportswear-appropriate presets (Sport Sans Bold, Block Serif, Classic Serif, Impact, Typewriter)
- Size slider with live numeric readout
- Color picker button that opens the Color Modal
- Three alignment toggle buttons: L / C / R

**Thumbnail Zone Indicators:**
Use the same thumbnail image style as the existing pattern/zone thumbnails throughout this section to visually show the user which part of the shirt their logo or text will be placed on. Labels must be plain, readable, and friendly — no technical terms.

All text, labels, and instructions in this section must be written in clear, simple language that any customer can understand without guidance.

---

**10. Step 4 — Sizing & Order Matrix**

**Mode toggle:** Two radio options at the top — "Standard Wholesale Sizes" and "Custom Specification Form"

**Standard mode table:**

| S | M | L | XL | XXL | RUNNING TOTAL |
|---|---|---|---|---|---|
| input | input | input | input | input | auto-sum Pcs |

Quantity inputs are number fields, minimum 0. The running total updates automatically on every keystroke.

**Custom Specification Form** (shown when toggled):
- Fields: Chest Width, Body Length, Shoulder Width, Sleeve Length, Total Quantity, Fit (Regular / Slim / Athletic / Relaxed), Special Notes textarea

**WhatsApp CTA Callout — always visible at the bottom of this step:**
> 💬 **Looking for Special Custom Tailoring or Ordering in Large Bulk Quantities?**
> We are happy to accommodate unique requirements. **Connect with Us Directly via WhatsApp** to receive custom quotes and dedicated design assistance.

WhatsApp button must open `https://wa.me/{number}?text=Hi Ka.Sha Golfwear! I'm interested in a custom bulk order.` in a new tab.

---

**11. Add to Cart — Full Customization Data Capture**

When the user clicks Add to Cart, capture and attach all of the following to the cart item:

- Product SKU
- Product type (Pattern / Print / Solid)
- Customization type selected (Color / Print / Pattern)
- Body color hex (if color was applied)
- Pattern panel color hex (if applicable)
- Selected print ID and name (if print was applied)
- Uploaded logo file reference and position (x, y coordinates within safe zone)
- Custom text content, font, size, color hex, and alignment
- Size quantities: S, M, L, XL, XXL and the total piece count

Display a summary card of all captured customization details to the user immediately after Add to Cart is clicked, before proceeding to checkout.