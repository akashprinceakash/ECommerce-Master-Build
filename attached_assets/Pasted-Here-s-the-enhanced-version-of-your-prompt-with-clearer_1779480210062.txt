Here’s the enhanced version of your prompt with clearer technical direction, improved UI instructions, and explicit functionality requirements:

Additionally, improve the visual hierarchy and interaction clarity throughout the customization experience.

────────────────────────────
TYPOGRAPHY & VISUAL IMPROVEMENTS
────────────────────────────

All newly added content, sections, buttons, labels, cards, and headings should have:

* darker font color for better readability
* slightly larger font sizes
* improved font weight
* cleaner spacing
* premium typography styling

The new UI elements should feel visually stronger and more premium compared to the existing design.

Ensure:

* section headings are bold and prominent
* card titles are more readable
* action buttons feel more noticeable
* customization labels are easier to understand
* popup/modal typography is modern and luxurious

────────────────────────────
PATTERN SELECTION & 3D MODEL SYNCHRONIZATION FIX
────────────────────────────

Currently, when selecting a Pattern T-Shirt product from the Product Page and navigating into the customization page, the selected pattern is NOT automatically applied on the 3D model.

This must be fixed.

REQUIRED BEHAVIOR:

When the user:

1. Selects a Pattern T-Shirt product
2. Clicks “Customize” or “Full Customisation”
3. Gets redirected to the customization page

Then:

* the exact selected pattern/design must automatically load and apply on the 3D t-shirt model immediately on page load
* the selected pattern thumbnail/card should already appear as active/selected
* the customization state should persist between pages
* the user should instantly see the same pattern they selected previously without needing to reselect it

The workflow should behave exactly like the functionality shown in the attached reference code/demo.

────────────────────────────
DEFAULT STATE MANAGEMENT REQUIREMENT
────────────────────────────

The customization page must maintain and preload the selected configuration automatically.

For example:

If the user selects:

* a specific pattern t-shirt
* a specific print design
* a specific product variant

Then after navigation:

* the same design should already be applied
* the same configuration should already be selected in the UI
* the 3D model should render with the selected texture/pattern immediately

There should be NO reset of the selected design during page transition.

────────────────────────────
QUICK PERSONALISATION & FULL CUSTOMISATION SELECTION FIX
────────────────────────────

Currently:
“Popular”
“Quick Personalisation”
“Full Customisation”

are appearing selected/active together by default.

This is incorrect.

REQUIRED FIX:

* Only the actually selected option should appear active.
* Remove multiple active states.
* Use proper toggle/selection state management.
* Add premium active-state styling.

Example:

* if user selects “Quick Personalisation” → only that card/button becomes active
* if user selects “Full Customisation” → deactivate others and activate only that option

Active state styling should include:

* stronger border glow
* darker premium background
* elevated shadow
* highlighted typography
* smooth transition animation

────────────────────────────
REFERENCE FUNCTIONALITY REQUIREMENT
────────────────────────────

Follow the same behavior demonstrated in the attached reference implementation/code:

Example expected flow:

1. User selects a Pattern T-Shirt
2. User clicks Customize
3. Navigation happens
4. Customization page opens
5. Selected pattern is already applied on the 3D model automatically
6. Pattern selection UI already shows that pattern as selected
7. User continues customization seamlessly

The experience should feel smooth, intelligent, state-persistent, and production-ready.
