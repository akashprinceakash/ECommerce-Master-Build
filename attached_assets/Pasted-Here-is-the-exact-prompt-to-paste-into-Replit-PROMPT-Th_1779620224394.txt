Here is the exact prompt to paste into Replit:

---

**PROMPT:**

The app is already built. Do not change the tech stack or rebuild anything from scratch. Apply only the following changes to the existing codebase:

---

**1. Admin — SKU-Based Design Upload System**

Add an admin panel where the admin can enter a SKU code and upload new pattern, print, or solid designs against that SKU. When a user later arrives at the customization page with that SKU, the system must automatically load the correct design registered to it. The SKU prefix determines the product type:

- SKU starting with `KP` = Pattern product
- SKU starting with `KS` = Print product
- Solid products = no prefix rule, but admin tags it as "Solid" during upload

All SKU-to-design mappings must persist so future admin uploads work immediately without code changes.

---

**2. Layout — Left Panel Fixed at 40%**

On large screens (desktop/tablet landscape), the live preview panel must occupy exactly **40% of the screen width** on the left side. The customization controls occupy the remaining 60% on the right. The full customization page must be fully responsive across all screen sizes — mobile, tablet, and desktop.

---

**3. Page Flow — Structural Navigation Based on Selected SKU Product Type**

The page flow shown to the user must change dynamically based on which SKU product they arrived from. Do not show a generic flow — show only what is relevant to their selected product type.

---

**FLOW A — User selects a PATTERN product (e.g. KP1001B)**

When the user lands on the customization page with a Pattern SKU, immediately load and render the design attached to that SKU in the live preview.

**Step 1 — Auto-loaded:** Pattern design renders from SKU. No selection needed.

**Step 2 — Customization Type Choice:** Show the user two clear options:

- **Option 1: Color Customization**
  → User can change the Base Body color OR the Pattern Panel color (collar, sleeves, accents) independently using the color picker modal.
  → Proceed to Step 3.

- **Option 2: Print Customization**
  → Display the print gallery (same as the existing Pattern 2 "Parts Print — Choose Pattern" UI). User selects a print to apply on top of the pattern panels.
  → Proceed to Step 3.

**Step 3 — Text & Logo** (same as current implementation)

**Step 4 — Sizing & Order** (same as current implementation)

---

**FLOW B — User selects a PRINT product (e.g. KS1000BGP001)**

When the user lands on the customization page with a Print SKU, immediately load and render the design attached to that SKU in the live preview.

**Step 1 — Auto-loaded:** Print design renders from SKU. No selection needed.

**Step 2 — Print Selection:** Show the full print gallery (gp001 through gp034, matching current functionality). User selects which print to apply.

**Step 3 — Text & Logo** (same as current implementation)

**Step 4 — Sizing & Order** (same as current implementation)

---

**FLOW C — User selects a SOLID product**

When the user lands on the customization page with a Solid SKU, immediately load and render the design attached to that SKU in the live preview.

**Step 1 — Auto-loaded:** Solid design renders from SKU. No selection needed.

**Step 2 — Customization Type Choice:** Show the user three clear options:

- **Option 1: Pattern Customization**
  → Follow the exact same flow as FLOW A (Pattern product flow) from Step 2 onward.

- **Option 2: Print Customization**
  → Follow the exact same flow as FLOW B (Print product flow) from Step 2 onward.

- **Option 3: Color Customization**
  → Show two sub-options:
  - **Full Body Color** — applies a single color across the entire shirt
  - **Parts Color** — allows the user to change individual zone colors (collar, sleeves, body panels) separately using the color picker modal

**Step 3 — Text & Logo** (same as current implementation)

**Step 4 — Sizing & Order** (same as current implementation)

---

**4. Text & Logo Section — UI Improvements**

Update the Text & Logo step to match the following requirements:

- Use **thumbnail images** (same style as the existing pattern/zone thumbnails) to visually show which part of the shirt is being targeted when the user interacts with text or logo placement.
- All text labels, instructions, and field labels in this section must be **clear, friendly, and easy to read** — no technical jargon, no abbreviations.
- The layout must feel guided — users should always know what zone they are editing.

---

**5. Add to Cart — Capture Full Customization Details**

When the user clicks "Add to Cart", capture and attach all of the following customization details to the cart item:

- SKU of the selected product
- Product type (Pattern / Print / Solid)
- Customization type chosen (Color / Print / Pattern)
- Body color hex value (if color customization was applied)
- Pattern panel color hex value (if applicable)
- Selected print ID or name (if print was applied)
- Uploaded logo file reference (if any)
- Logo position within the chest safe zone (x, y coordinates)
- Custom text content
- Text font, size, color, and alignment
- Size quantities (S, M, L, XL, XXL) with running total

All of this data must be stored with the cart item so it can be retrieved for order processing. Display a summary of the customization to the user before or after they click Add to Cart.

---

**6. Global Rule — Always Show Based on SKU**

Everything shown to the user on the customization page — the design, the step flow, the options, the customization controls — must be driven entirely by the SKU that was passed in from the product page. Never show options that are irrelevant to the selected product type.